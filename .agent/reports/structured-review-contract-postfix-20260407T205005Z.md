- [P1] Active YouTube session data can be dropped when user clicks Stop (confidence: High).  
  `STOP_CAPTURE` immediately disables capture ([youtube-observer.ts:133](/Users/jonas/Documents/RESMA-main/extension/src/content/youtube-observer.ts:133)), but `finalizeActiveVideo()` now uploads only if capture is still active ([youtube-observer.ts:535](/Users/jonas/Documents/RESMA-main/extension/src/content/youtube-observer.ts:535)). If the user stops while still on a video, that in-progress video is never uploaded (no flush on stop), causing silent data loss/regression.

- [P1] Replay/idempotency path introduces unbounded in-memory response retention under normal traffic (confidence: High).  
  All three ingest routes now wrap writes in replay guard ([feeds.ts:84](/Users/jonas/Documents/RESMA-main/backend/src/routes/feeds.ts:84), [youtube.ts:213](/Users/jonas/Documents/RESMA-main/backend/src/routes/youtube.ts:213), [instagram.ts:204](/Users/jonas/Documents/RESMA-main/backend/src/routes/instagram.ts:204)); the extension now sends a unique upload id on every request ([service-worker.ts:385](/Users/jonas/Documents/RESMA-main/extension/src/background/service-worker.ts:385)). The guard stores completed responses in process memory for 10 minutes with no cap ([ingestReplayGuard.ts:10](/Users/jonas/Documents/RESMA-main/backend/src/services/ingestReplayGuard.ts:10)), so memory grows with ingest rate; `/feeds` is especially heavy because cached response bodies include full snapshot payloads.

- [P2] Invalid explicit `platform` values are silently coerced to `tiktok` on `/feeds` (confidence: High).  
  `/feeds` calls coercion with `defaultPlatform: 'tiktok'` ([feeds.ts:72](/Users/jonas/Documents/RESMA-main/backend/src/routes/feeds.ts:72)); shared coercion resolves platform as `parsePlatform(payload.platform) ?? defaultPlatform` ([index.ts:476](/Users/jonas/Documents/RESMA-main/packages/shared/src/index.ts:476)). A malformed/non-supported platform string is therefore accepted and persisted as TikTok instead of rejected, creating cross-platform data contamination.
