{
  "name": "@resma/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server for aggregate-only RESMA analysis endpoints",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "bun --watch run src/index.ts",
    "start": "bun run dist/index.js",
    "typecheck": "tsgo"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.5",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
