{
  "name": "resma",
  "version": "0.1.0",
  "private": true,
  "description": "Reverse Engineering Social Media Algorithms",
  "workspaces": [
    "extension",
    "backend",
    "frontend",
    "packages/shared",
    "packages/mcp",
    "packages/headless"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test",
    "lint": "bun run --filter '*' lint",
    "format": "prettier --write .",
    "typecheck": "tsgo --build --builders 4 --checkers 4"
  },
  "devDependencies": {
    "@typescript/native-preview": "latest",
    "prettier": "^3.2.0",
    "typescript": "^5.3.0"
  }
}
